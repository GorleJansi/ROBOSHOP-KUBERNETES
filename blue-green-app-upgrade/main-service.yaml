apiVersion: v1
kind: Service
metadata:
  name: "main-service"
  namespace: roboshop
spec:
  selector:
    app: roboshop
    color: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
