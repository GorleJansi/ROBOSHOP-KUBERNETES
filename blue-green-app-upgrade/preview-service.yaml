apiVersion: v1
kind: Service
metadata:
  name: "preview-service"
  namespace: roboshop
spec:
  selector:
    app: roboshop
    color: green
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
